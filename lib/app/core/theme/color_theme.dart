import 'package:flutter/material.dart';

abstract class FGBPColors {
  /// Main Color
  static const mainColor = Color(0xff0E4A84);

  /// Sub Color
  static const subColor = Color(0xff898C8E);

  /// Accent Color
  static const accentColor = Color(0xffD8A300);

  /// black
  static const black = Color(0xff1F272B);

  /// white
  static const white = Colors.white;

  /// grey
  static const grey = Color(0xff333C45);
}

abstract class FGBPDarkColors {
  /// Main Color
  static const mainColor = Color(0xff0E4A84);

  /// Sub Color
  static const subColor = Color(0xff898C8E);

  /// Accent Color
  static const accentColor = Color(0xff6F5113);

  /// black
  static const black = Color(0xff121212);

  /// white
  static const white = Color(0xffE0E0E0);

  /// grey
  static const grey = Color(0xff424242);
}
